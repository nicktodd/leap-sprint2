services:
  app:
    build: .
    depends_on:
      - db

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: sprint2_db
      POSTGRES_PASSWORD: sprint2_lab_password
    ports:
      - "5432:5432"
